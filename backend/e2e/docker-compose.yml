# E2E-Umgebung: ECHTES Zitadel + Postgres — keine Mocks.
# Das Backend läuft als echtes Binary auf dem Host (siehe e2e_test.go),
# damit sein AUTH_ISSUER (http://localhost:8123) mit dem Token-Issuer
# von Zitadel übereinstimmt.
#
# Start:  docker compose up -d --wait   (dauert beim ersten Mal ~1 min)
# Tests:  cd .. && go test -tags e2e ./e2e/ -v
#
# ZITADEL_EXTERNALDOMAIN/-PORT sind übersteuerbar, weil OIDC verlangt, dass
# ALLE Beteiligten denselben Aussteller sehen. Für den Android-E2E ist das
# nicht „localhost": der Emulator erreicht den Host ausschließlich unter
# 10.0.2.2, also läuft Zitadel dort mit
#
#   ZITADEL_EXTERNALDOMAIN=10.0.2.2 docker compose up -d --wait
#
# und der Runner legt sich 10.0.2.2 als Loopback-Adresse an, damit auch das
# Backend auf dem Host denselben Aussteller auflöst
# (siehe .github/workflows/android.yml).
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 3s
      timeout: 3s
      retries: 20

  zitadel:
    image: ghcr.io/zitadel/zitadel:v2.65.1
    depends_on:
      db:
        condition: service_healthy
    command: start-from-init --masterkey "MasterkeyNeedsToHave32Characters" --tlsMode disabled
    environment:
      ZITADEL_DATABASE_POSTGRES_HOST: db
      ZITADEL_DATABASE_POSTGRES_PORT: "5432"
      ZITADEL_DATABASE_POSTGRES_DATABASE: zitadel
      ZITADEL_DATABASE_POSTGRES_USER_USERNAME: zitadel
      ZITADEL_DATABASE_POSTGRES_USER_PASSWORD: zitadel
      ZITADEL_DATABASE_POSTGRES_USER_SSL_MODE: disable
      ZITADEL_DATABASE_POSTGRES_ADMIN_USERNAME: postgres
      ZITADEL_DATABASE_POSTGRES_ADMIN_PASSWORD: postgres
      ZITADEL_DATABASE_POSTGRES_ADMIN_SSL_MODE: disable
      ZITADEL_EXTERNALDOMAIN: ${ZITADEL_EXTERNALDOMAIN:-localhost}
      ZITADEL_EXTERNALPORT: "${ZITADEL_EXTERNALPORT:-8123}"
      ZITADEL_EXTERNALSECURE: "false"
      # Beim Init einen IAM-Admin-Machine-User anlegen und dessen
      # JSON-Key nach /machinekey schreiben — damit bootstrappen die Tests.
      ZITADEL_FIRSTINSTANCE_MACHINEKEYPATH: /machinekey/zitadel-admin-sa.json
      ZITADEL_FIRSTINSTANCE_ORG_MACHINE_MACHINE_USERNAME: zitadel-admin-sa
      ZITADEL_FIRSTINSTANCE_ORG_MACHINE_MACHINE_NAME: E2E Admin
      ZITADEL_FIRSTINSTANCE_ORG_MACHINE_MACHINEKEY_TYPE: "1"
      # Für den Healthcheck (`zitadel ready` liest die Env, nicht die CLI-Flags):
      ZITADEL_TLS_ENABLED: "false"
    ports:
      # Bewusst auf allen Adressen des Hosts: Der Android-E2E spricht dieselbe
      # Instanz unter 10.0.2.2 an (Loopback-Alias auf dem Runner).
      - "${ZITADEL_EXTERNALPORT:-8123}:8080"
    volumes:
      - ./machinekey:/machinekey
    healthcheck:
      test: ["CMD", "/app/zitadel", "ready"]
      interval: 5s
      timeout: 5s
      retries: 40
      start_period: 30s
