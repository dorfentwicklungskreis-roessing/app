<resources>
    <!-- Compose übernimmt das Theming; hier nur die System-Splash-Basis. -->
    <style name="Theme.DorfApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
